name: 1_First job

on:
  workflow_call:
    inputs:
      environment:
        type: string
        required: true
        description: enter enbvironment vale

      space:
        type: string
        required: true
        description: enter space value



jobs:
  run_ui_log:
    environment: ${{ inputs.environment }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.10.8
      - name: List of Dependencies after installing python
        run: | 
          pip list
      - name: Install pip
        run: |
          python -m pip install --upgrade pip
      - name: Install dependencies
        run : |
          pip install -e ".[dev]"
      - name: List installedDependencies
        run: |
          pip list
      - name: List installedDependencies With Their Installed Path
        run: |
          pip list -v
      - name: Run ui log python file
        run: |
          python ./me/personal/functs/run_code_ui_log.py
  run:
    needs: run_ui_log
    environment: ${{ inputs.environment }}
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.10.8
      - name: Install pip
        run: |
          python -m pip install --upgrade pip
      - name: Install dependencies
        run: |
          pip install -e ".[dev]"
      - name: List installedDependencies With Their Installed Path
        run: |
          pip list -v
      - name: Run file based python code
        run: |
          ls /home/runner/work/c-python-pri-with-logging-and-typing/c-python-pri-with-logging-and-typing/
          python ./me/personal/log-examples/run_code_file_log.py
          
          



